

## Replace "Explore Courses" with "Explore Ranges" in Hero

### Change
Update the first CTA button in the hero section to say **"Explore Ranges"** and link to `/ranges` instead of `/courses`.

### Technical Detail

**File: `src/components/Hero.tsx`** (around line 131)

- Change the button text from "Explore Courses" to "Explore Ranges"
- Change the `Link to` from `/courses` to `/ranges`

